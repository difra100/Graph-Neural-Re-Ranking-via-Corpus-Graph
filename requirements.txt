# Pinned to the known-good versions from the original working environment.
# IMPORTANT: the package that provides `import pyterrier` is `python-terrier`.
# The terrierteam plugins MUST stay on the 0.0.1 line: newer releases (e.g.
# pyterrier-adaptive 0.2.1) require pyterrier>=1.0 and break this codebase.

# Core IR stack (terrierteam).
# NOTE: pyterrier-{adaptive,dr,t5}==0.0.1 are NO LONGER on PyPI (newer releases
# require pyterrier>=1.0 / python>=3.9 and break this codebase). setup_env.sh vendors
# those three pure-Python plugins from the known-good source env instead.
python-terrier==0.10.0
ir_measures==0.3.3
pytrec_eval_terrier==0.5.6   # 0.5.10 breaks on py3.8 ('ABCMeta' object is not subscriptable)
npids==0.0.3
faiss-cpu==1.7.4

# Transformers stack used by pyterrier_dr (TctColBert) and pyterrier_t5
transformers==4.30.2
sentence-transformers==2.2.2

# Data / numerics
numpy==1.23.4
pandas==1.3.5
scipy==1.8.0
scikit-learn==1.1.3

# Training / logging
pytorch-lightning==1.5.10
torchmetrics==0.10.0
wandb==0.15.4
matplotlib==3.7.5
networkx==3.0

# NOTE: torch / torch_geometric / torch_scatter / torch_sparse are installed
# separately in scripts/setup_env.sh (CUDA-specific wheels, see README).
# allrank is OPTIONAL: src/lightningmodule.py ships a LambdaRank fallback when absent.
